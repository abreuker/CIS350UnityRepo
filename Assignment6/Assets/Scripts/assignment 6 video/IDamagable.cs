using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
 * Anna Breuker
 * Assignment 6
 * An interface that ensures the class implements the TakeDamage method.
 */
public interface IDamagable
{
    void TakeDamage(int amount);
}
